import React, { useState } from 'react';
import Post from './Post';
import '../style.css';

const Form = () => {
	return (
		<div className="form-content">
			<Post />
		</div>
	);
};

export default Form;
